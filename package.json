{
  "name": "finlearn",
  "version": "0.8.0",
  "description": "FinLearn Packages",
  "license": "ISC",
  "workspaces": ["client", "server"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "rimraf": "^5.0.1"
  }
}
